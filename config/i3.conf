# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace, FontAwesome, 9

# Gaps between containers
gaps inner 14
# Gaps between screen edges and containers
#gaps outer 5
#gaps horizontal 100
#gaps vertical 500

# Create only gaps if more then one container
#smart_gaps on

#new_float none
new_window pixel 0

default_border pixel 1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec gnome-terminal

# kill focused window
bindsym $mod+Shift+q kill

# Rofi bindings (launcher)
bindsym $mod+d exec --no-startup-id rofi -combi-modi window#drun -show combi -modi combi -show-icons
bindsym $mod+Tab exec --no-startup-id rofi -show window

# Change focused window
bindsym $mod+Left exec i3-msg border pixel 1; focus left
bindsym $mod+Down exec i3-msg border pixel 1; focus down
bindsym $mod+Up exec i3-msg border pixel 1; focus up
bindsym $mod+Right exec i3-msg border pixel 1; focus right
bindsym --release $mod+Left exec i3-msg border pixel 1
bindsym --release $mod+Down exec i3-msg border pixel 1
bindsym --release $mod+Up exec i3-msg border pixel 1
bindsym --release $mod+Right exec i3-msg border pixel 1

# Move focused window
bindsym $mod+Shift+Left exec i3-msg border pixel 1; move left
bindsym $mod+Shift+Down exec i3-msg border pixel 1; move down
bindsym $mod+Shift+Up exec i3-msg border pixel 1; move up
bindsym $mod+Shift+Right exec i3-msg border pixel 1; move right
bindsym --release $mod+Shift+Left exec i3-msg border pixel 0
bindsym --release $mod+Shift+Down exec i3-msg border pixel 0
bindsym --release $mod+Shift+Up exec i3-msg border pixel 0
bindsym --release $mod+Shift+Right exec i3-msg border pixel 0

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# Define names for workspaces
set $ws1    "1:   Mail "
set $ws2    "2:   Terminal "
set $ws3    "3:   Firefox "
set $ws4    "4:   Code "
set $ws5    "5:   Files "
set $ws6    "6:   Media "
set $ws7    "7:   Design "
set $ws8    "8:   Chat "
set $ws9    "9:   General "
set $ws10   "10:   Settings "

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# assign program to workspace
assign [class="Firefox"] → $ws3
assign [class="Thunderbird"] → $ws1
assign [class="libreoffice"] → $ws5

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# Move Workspace to other monitor
bindsym $mod+Mcod1+Left move workspace to output left
bindsym $mod+Mod1+Right move workspace to output right

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# class                 border  backgr. text    indicator child_border
client.focused          #2b83a6 #2b83a6 #ffffff #dddddd   #2b83a6
client.focused_inactive #212121 #212121 #86888c #292d2e   #5a5a5a
client.unfocused        #212121 #212121 #86888c #292d2e   #5a5a5a
client.urgent           #d64e4e #d64e4e #ffffff #d64e4e   #d64e4e
client.placeholder      #212121 #0c0c0c #ffffff #212121   #262626
client.background       #212121

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        position top
        i3bar_command i3bar -t
        status_command SCRIPT_DIR=~/.config/i3/scripts i3blocks -c ~/.config/i3/i3blocks.conf
        tray_output none
        workspace_buttons yes
        #strip_workspace_numbers yes
        separator_symbol "|"
        colors {
          #background  #2121216f
          background  #212121
          statusline  #dddddd
          separator   #666666
          # class             border  backgr. text
          focused_workspace   #2b83a6 #2b83a6 #ffffff
          active_workspace    #212121 #212121 #ffffff
          inactive_workspace  #212121 #212121 #86888c
          urgent_workspace    #d64e4e #d64e4e #ffffff
          binding_mode #229c56 #229c56 #ffffff
        }
}

# Create Log out, Reboot, Poweroff bindings
set $lockModeName "Exit (L)ock, (R)eboot, (P)oweroff, (E)xit"
mode $lockModeName {
    bindsym $mod+Shift+r exec systemctl reboot
    bindsym $mod+Shift+l mode "default"; exec "/usr/bin/i3lock-fancy -n -- scrot"
    bindsym $mod+Shift+e exec gnome-session-quit --no-prompt --logout
    bindsym $mod+Shift+p exec systemctl poweroff

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+e mode $lockModeName
bindsym $mod+l exec "/usr/bin/i3lock-fancy -n -- scrot"

# Screenshot Mode
bindsym --release $mod+Shift+s exec "notify-send -t 2000 -- 'Select region for capture' && \
 scrot -s ~/Pictures/capture_$(date +%Y_%m_%d-%H_%M_%S).png && \
 wait && \
 if [ $? -eq 0 ]; then nautilus ~/Pictures; fi"

# control focused window when follows the mouse movements
focus_follows_mouse no

exec --no-startup-id gnome-flashback
exec --no-startup-id compton --config ~/.config/compton/config
exec --no-startup-id xautolock -time 5 -locker "/usr/bin/i3lock-fancy -n -- scrot" -detectsleep -notify 30 -notifier "notify-send -u critical -t 10000 -- 'Locking screen in 30 seconds'"
exec --no-startup-id thunderbird
exec --no-startup-id firefox
exec --no-startup-id ~/virtualenv/mopidy/bin/mopidy
exec --no-startup-id xrandr --output DP-1-2-3 --right-of DP-1-2-2 --auto
#exec --no-startup-id dunst

# scratchpad
# Move window to scratchpad
bindsym $mod+Shift+BackSpace move scratchpad
# Cycle through windows in scratchpad
bindsym $mod+u scratchpad show
# Show specific window in scratchpad via WM_CLASS; find out with xprop
bindsym $mod+k [class="KeePass2"] scratchpad show

for_window [class="Yad"] floating enable
